stages:
  - build
  - test

build:
  stage: build
  image: gradle:8.5-jdk17
  script:
    - ./gradlew clean build

test:
  stage: test
  image: node:18
  script:
    - npm install -g newman
    - newman run 1dv027-API.postman_collection.json